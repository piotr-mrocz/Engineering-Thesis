export interface UserPresentsPerDayDto {
    userName?: string,
    idUser?: number,
    isPresent?: boolean,
    presentType?: number,
    absenceReason?: string,
    startTime?: string,
    endTime?: string
}
